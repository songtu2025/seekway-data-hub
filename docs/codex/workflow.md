# Codex 开发流程与项目边界

当任务涉及代码、项目结构、配置、依赖、部署、项目文档或生产边界时，必须读取本文件。本文是根目录 `AGENTS.md` 的专项扩展；两者冲突时必须停止并修正规则，不得自行选择其一。

## 1. 技术与环境基线

以下是新项目默认基线；历史项目以已经确认的实际技术栈和运行环境为准，并在 README 或根目录 `AGENTS.md` 中记录差异：

- 开发环境：Windows
- 代码托管：GitHub
- 生产环境：阿里云 ECS
- 前端：React 19、TypeScript、Vite；保留项目现有基础组件和主题，不因公司新项目默认值迁移 Ant Design
- 后端：Python、FastAPI、Pydantic、SQLAlchemy、Alembic、pytest、Ruff，以及项目选择的 Mypy 或 Pyright
- 新项目默认部署：Docker、Docker Compose、Nginx；本项目使用 ECS、Docker Compose 和宿主机 Nginx/Certbot

未经明确批准，不得更换项目已经采用的前端或后端框架、数据库访问或迁移方案、部署方式，不得引入新的基础设施组件或生产依赖。

## 2. 标准项目结构

新项目默认采用前后端分离、同一仓库管理的结构：

```text
project-name/
├── AGENTS.md
├── README.md
├── .env.example
├── .gitignore
├── compose.yaml
├── docs/
│   └── codex/
├── frontend/
│   ├── package.json
│   ├── src/
│   └── tests/
├── backend/
│   ├── pyproject.toml
│   ├── alembic.ini
│   ├── app/
│   ├── migrations/
│   └── tests/
├── deploy/
└── .github/
    ├── workflows/
    ├── ISSUE_TEMPLATE/
    ├── pull_request_template.md
    └── CODEOWNERS
```

新项目必须遵守：

1. 前端代码放在 `frontend`，后端代码放在 `backend`，项目文档放在 `docs`。
2. 数据库迁移放在 `backend/migrations`，部署配置放在 `deploy` 或根目录明确位置，GitHub 配置放在 `.github`。
3. 未经批准不得改变一级目录结构；项目不需要的二级目录可以省略。
4. 不得提交临时文件、日志、数据库文件和真实业务数据。

历史项目不受上述目录命名约束，必须优先保持现有结构和兼容性；不得仅为符合模板进行无业务收益的全量重构。

## 3. 既有项目规范接入

项目已有 `AGENTS.md`、`AGENTS.override.md`、嵌套规则、贡献指南或等价开发规范时，不得使用公司模板整文件覆盖。公司规范是默认基线，项目已经验证的业务、架构和工具约束是项目事实；接入属于非简单任务，必须先完成审计并获得确认。

### 3.1 只读审计

修改规则前必须：

1. 读取项目根目录和相关子目录中的 `AGENTS.md`，确认每个文件的作用域。
2. 检查是否存在 `AGENTS.override.md`、备用规则文件、README、贡献指南和专项规范。
3. 从代码、配置和自动化入口核对实际技术栈、目录、启动方式、检查命令和部署方式。
4. 列出应保留、重复、疑似过时、互相冲突、目录特有、低频专项和公司规范缺失的规则。
5. 说明准备修改的文件、迁移方式、风险和验证方法；无法确认的规则必须保持不变并请求确认。

### 3.2 分类与合并

| 规则类型 | 处理方式 |
| --- | --- |
| 项目业务事实、架构边界、禁止事项和真实检查命令 | 保留，并以项目实际情况为准 |
| 与公司规范含义相同的规则 | 只保留一个事实来源，删除重复表述 |
| 已由项目工具稳定强制执行的格式和静态规则 | 保留工具、配置和执行命令，不在始终加载的文件中逐条复述 |
| 只影响一个目录的规则 | 放入该目录已有或必要的嵌套 `AGENTS.md`，不得在根文件重复 |
| 低频或任务专项流程 | 放入现有专项文档，并在根文件说明准确的读取条件 |
| 有证据确认已经失效的规则 | 说明证据和影响后删除 |
| 无法确认真伪的规则 | 保留并请求确认，不得自行删除 |
| 项目不使用的公司默认技术或流程 | 不复制，并在必要时记录为不适用或项目差异 |

合并时必须补充缺失的公司安全和生产底线，但不得机械追加完整模板、改变已确认的技术栈或为了目录统一进行全量重构。

### 3.3 冲突处理

1. 法律合规、数据安全、权限、生产数据库、生产发布和破坏性操作采用更严格规则，不得通过项目差异放宽。
2. 项目实际技术栈、目录、包管理器、测试框架、部署方式和已验证设计系统优先于公司默认值。
3. 根规则与子目录规则冲突时，子目录规则只在其目录作用域内优先，并应明确说明例外原因。
4. 无法同时满足或证据不足的冲突必须列出双方规则、项目证据、影响范围和建议，获得确认后处理。

### 3.4 规则文件边界

父级规则会与路径上的子级规则累积，嵌套 `AGENTS.md` 只补充局部差异，不得重复父级规则。只有目录确实具有独立命令、架构或安全约束时才创建嵌套规则。

同一目录存在 `AGENTS.override.md` 时，Codex 只选择该文件而不再加载同目录的普通 `AGENTS.md`。它只用于明确的替换场景，不得用于追加公司规则；发现已有文件时，未经确认不得新增、删除或替换。

全局 `AGENTS.md` 只适合保存稳定的跨项目偏好和底线；项目正确交付所需的业务、架构、检查和安全规则必须随仓库维护，不得依赖某位开发者的全局配置。

`docs/codex` 等普通 Markdown 文件不会仅因存在而自动加入规则链，根文件必须用“触发条件 + 具体路径”说明何时读取。项目已有等价文档时应索引现有来源，不得复制第二份；没有同步某项专项规范时，必须同时删除或调整指向它的索引。

### 3.5 规模、版本与升级

新项目根目录 `AGENTS.md` 以 100 行以内为目标。历史项目超过目标时必须先消除重复和失效内容，再按作用域和加载频率渐进迁移；不得把行数作为删除有效规则或阻塞必要修改的唯一理由。

Codex 当前默认最多组合 32 KiB 项目规则。接近限制时应优先减少重复、缩小始终加载范围或使用合理的目录分层，不得默认提高限制掩盖上下文膨胀；加载行为以 [Codex 官方说明](https://learn.chatgpt.com/docs/agent-configuration/agents-md) 为准。

项目 README 必须记录公司规范基线版本、接入日期和项目差异。公司规范升级时，应将变更分类为适用、不适用、已存在和冲突，只合并适用且缺失的内容；完成后更新基线版本，不得重新复制覆盖。规范版本使用语义化版本，已经发布的版本不得原地改写。

## 4. 开始开发前

修改代码前必须：

1. 阅读根目录和相关子目录中的 `AGENTS.md`，并按其中索引加载与任务有关的专项规范。
2. 阅读 README、需求文档、相关代码和测试。
3. 检查相关调用链、现有实现方式和可复用能力。
4. 复述需求、验收标准和明确不做的事项。
5. 列出计划修改的文件及修改内容。
6. 说明是否涉及数据库、认证、权限、配置、依赖或部署。
7. 识别风险和回滚方式，给出测试与验证方案。
8. 非简单任务在获得用户确认前不得开始修改。

涉及 Web 界面时，还必须读取 `docs/codex/frontend.md` 和项目的 `docs/web-ui-standard.md`。本项目继续由该单文件维护布局、表单和数据展示规则，不因 V1.7.0 自动拆分。随后检查已有设计令牌、基础组件、页面布局和相似页面，并明确设计来源、目标视口及各页面状态。

## 5. 修改范围

必须：

1. 只修改完成当前任务所必需的文件，保持现有业务规则、接口和兼容性。
2. 优先复用已有代码和实现模式，新增或修改业务逻辑时补充必要测试。
3. 发现无关问题时只记录，不顺带修改。

不得：

1. 自行增加功能、页面、接口、业务规则或需求中没有的业务字段。
2. 进行与当前任务无关的重构或修改无关文件。
3. 擅自删除已有功能、接口、字段或数据。
4. 擅自修改公共模块、认证、权限或部署配置。
5. 为“以后可能使用”提前增加代码、字段或组件。
6. 在多个位置复制相同业务规则。

## 6. 安全与生产边界

不得主动读取、生成、提交、输出或传播：

- 真实密码、Token、AccessKey、SecretKey 和私钥。
- 生产环境变量的真实值。
- 未脱敏的真实业务数据。
- 包含真实敏感信息的数据库文件、日志、上传文件和导出结果。

可以读取和处理：

- 认证相关的代码、字段名和配置结构。
- 不包含真实凭据的 `.env.example` 和占位值。
- 测试数据、模拟数据和已经脱敏的数据。
- 用户明确提供且不包含真实敏感信息的需求附件、示例文件和设计资料。

允许使用不显示命中值的自动化工具扫描源码和提交记录。意外发现真实敏感信息时，应停止处理相关内容并提醒用户，不得在日志或回复中复述其值。不得直接修改生产数据库或自动执行生产迁移；生产操作只能由部署负责人执行。

## 7. 项目文档

创建或维护项目时，README 至少包含：

- 项目用途、适用范围和明确不做的事项。
- 技术栈、系统架构和关键业务规则。
- 本地启动、测试和构建方式。
- 部署文档入口。

根目录 `AGENTS.md` 至少包含业务边界、Codex 修改规则、禁止修改内容、专项规范索引、必须执行的检查以及数据和 Git 安全要求。重要业务规则不得只存在于聊天记录或个人记忆中。
